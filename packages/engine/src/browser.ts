// @red-binder/engine/browser — the ONLY browser-coupled export, kept in its own entry so
// the core stays pure (no window/fs/network).
//
// Phase 1 scaffold placeholder. `downloadResults` lands in build phase 2, per
// docs/ARCHITECTURE.md §3.
export {};
