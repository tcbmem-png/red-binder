// Phase 1 scaffold placeholder. The real UI — document picker, entered-once shared core,
// conditional POA / Pocket Plan / RBP sections, review → generate → download, branding and
// bilingual microcopy — lands in build phases 4–7. Intentionally unstyled for now.
export function App() {
  return (
    <main
      style={{
        fontFamily: 'system-ui, sans-serif',
        maxWidth: 640,
        margin: '4rem auto',
        padding: '0 1rem',
      }}
    >
      <h1>Red Binder Project</h1>
      <p>A plan, before the emergency.</p>
      <p>
        Builder app — scaffold in progress. Everything will happen on your device; nothing is stored
        or sent.
      </p>
    </main>
  );
}
