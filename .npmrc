# Flat (npm-like) node_modules so root-level tooling (tsc, eslint, prettier, vitest, turbo)
# is available to every workspace package's scripts without duplicating dev deps.
node-linker=hoisted
auto-install-peers=true
